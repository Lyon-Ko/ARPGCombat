# Astra Medium — bounded final VFX repair

You are the implementation worker for D:/UEproject/Combat. Root Astra Ultra coordinates and reviews. User requested an unattended complete ARPG demo, realistic Kwang player/Greystone boss. Work only in Source/CombatEditor, Tools/editor, Content/Combat. Do not edit Source/Combat, Config, tests, or original Paragon assets. Do not build, restart, or commit. UE5.8.2 is D:/UE5.8/UE_5.8. Root owns build coordination.

CURRENT REMOTE LOCK: regression worker exclusively owns Unreal remote/Python/PIE. You MUST NOT call remote or touch the running editor until root writes REMOTE GRANTED in Docs/VFXCoordinator.md. Start with useful offline diagnosis/helper/script preparation. Report readiness to Docs/VFXProgress.md with latest message at top. Poll coordinator with waits under60seconds, avoid busy loops.

Actual root native PIE visual finding after Build_CancelUI_08: hit/special attack particle systems still emit huge soft orange spheres and white arrow-shaped particles that climb 3–5 meters above characters. They visibly look like Niagara demonstration particles, unsuitable final sword combat. Existing ConfigureNiagara only binds sprite and ribbon user Color/Size; inspect actual templates/helper to find mesh renderers/debug arrows and inactive bindings. Do not merely rename assets or hide all VFX.

Required result: concise believable orange metal hit sparks, a bright short cyan precise-parry flash, a sharp AOE release flash tied to the existing .18s release window, thin cyan/ember blade ribbons, and existing authored crescent swordwave + grounded warning ring. No floating demonstration arrows, giant blurry orbs, unnecessary cast burst at start of every ordinary swing, or lingering emitters. Source/Combat/Private/CombatFeedback.cpp shows current asset call sites; if runtime changes truly needed, report the minimal request to root instead of editing that owner's files. Prefer contained editor helper and asset changes.

Already implemented: native montage/socket/2D movement correctness, all19 skills actual damage, editable BP graphs, 33BP/39montage reload, real StateTree, independent tag extension. Do not regress these. Original crescent/ring assets in /Game/Combat/VFX and material graph scripts; real LocationBasedRibbon in generate_blade_ribbons.py. Root just committed Content/Combat and Tools/editor as4f86b31. Docs/EditorProgress.md is historical integration evidence. Current running Source includes real cancellation fix and new pause panel; binaries build08.

If helper changes are necessary, inspect installed UE headers and create a small robust editor-only API, confine mutation to /Game/Combat. Compile static/local if useful but root does full UBT after test release. Mark READY FOR BUILD and wait. Asset authoring will follow root build/restart and explicit remote handoff. Use Unreal native Python through Tools/Combat.ps1, no GDI/PowerShell UI automation. For actual screenshots use UE Shot SHOWUI / AutomationLibrary into Saved/Acceptance. Save before/after images of hit/parry/AOE/wave, report actual observed emitter properties, compile/save/reload. Root will visually review rendered screenshots and native window. Never claim unseen visual quality.

Need actual in-game audio output evidence too if a simple engine capture route exists: documented AudioMixer debug output recording or runtime SubmixRecording through reflected official API, no Windows loopback hacking. Read source and show root safe available route; do not add runtime hooks solely for tests. This is secondary to VFX quality, not a new feature.

Use model Astra Medium. Remain within this bounded repair and release remote when complete. No user questions. No tool permission overrides. Keep commentary/report concise Chinese. Task is not whole-demo final acceptance.
